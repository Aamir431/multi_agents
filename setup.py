from setuptools import setup, find_packages

setup(
    name='MULTI_AGENTS',  # The name of your package (how you will import it)
    version='1.0.0',       # The initial version of your project
    packages=find_packages(), # This automatically finds all packages in the folder
)